apiVersion: v1
kind: Service
metadata:
  name: transaction-validator
  labels:
    app: transaction-validator
spec:
  type: ClusterIP
  selector:
    app: transaction-validator
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
